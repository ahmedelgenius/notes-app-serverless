before_build:
  - echo 'export CI=false' >> $BASH_ENV
  - cd frontend && npm install

before_compile:
  - echo "Before compile"

#compile:
#  - echo "Override compile"

# before_build:
#   - echo "Before build"

before_deploy:
  - echo "Before deploy"

after_deploy:
  - echo "After deploy"

before_remove:
  - echo "Before deploy"

after_remove:
  - echo "After deploy"
